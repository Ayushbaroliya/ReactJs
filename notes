1.package .jason is the configuration for the npm 
2.bundler like veet parcel webpack also are bundler ye dare files ko production ready banate h 
3. npm installl -D this -D is the parameter there are two type of dependencies one is general one is dev dependencies dev dependencies are the dependencies which is requied in the development and general used in production also 
4.